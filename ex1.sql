CREATE TABLE person (
	id INTEGER PRIMARY KEY,
	first_name TEXT,
	last_name TEXT,
	age INTEGER
);

-- an ID column is used to exactly identify each row
-- in addition, specify that this column is primary key 